name: Panel Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'apps/panel/**'
      - 'packages/contracts/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'package.json'
      - 'tsconfig.json'
      - '.github/workflows/panel-pipeline.yml'
  workflow_dispatch:

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image_repo: ${{ steps.meta.outputs.image_repo }}
      image_tag: ${{ steps.meta.outputs.image_tag }}
      image_latest: ${{ steps.meta.outputs.image_latest }}
      short_sha: ${{ steps.meta.outputs.short_sha }}
      image_digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Resolve image metadata
        id: meta
        run: |
          set -euo pipefail
          OWNER_LOWER=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
          IMAGE_REPO="ghcr.io/${OWNER_LOWER}/digital-signage-panel"
          SHORT_SHA="$(echo "${GITHUB_SHA}" | cut -c1-8)"

          echo "image_repo=${IMAGE_REPO}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_REPO}:${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          echo "image_latest=${IMAGE_REPO}:latest" >> "$GITHUB_OUTPUT"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/panel/Dockerfile
          push: true
          tags: |
            ${{ steps.meta.outputs.image_tag }}
            ${{ steps.meta.outputs.image_latest }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Publish image metadata artifact
        run: |
          set -euo pipefail
          cat > panel-image.txt <<EOF
          image_repo=${{ steps.meta.outputs.image_repo }}
          image_tag=${{ steps.meta.outputs.image_tag }}
          image_latest=${{ steps.meta.outputs.image_latest }}
          digest=${{ steps.build.outputs.digest }}
          commit=${{ github.sha }}
          EOF

      - name: Upload image metadata artifact
        uses: actions/upload-artifact@v4
        with:
          name: panel-image-${{ steps.meta.outputs.short_sha }}
          path: panel-image.txt

  deploy:
    runs-on: ubuntu-latest
    needs:
      - build-and-push-image
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create env file
        run: |
          set -euo pipefail
          cat > ./envfile <<EOF
          PANEL_IMAGE=${{ needs.build-and-push-image.outputs.image_tag }}
          EOF

      - name: Configure SSH key
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.ssh"
          chmod 700 "$HOME/.ssh"
          printf '%s\n' '${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}' > "$HOME/.ssh/id_rsa"
          chmod 600 "$HOME/.ssh/id_rsa"
          ssh-keyscan -H '${{ secrets.DEPLOY_HOST }}' >> "$HOME/.ssh/known_hosts"

      - name: Configure remote Docker context
        run: |
          set -euo pipefail
          docker context rm remote || true
          docker context create remote --docker "host=ssh://${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}"
          docker --context remote version

      - name: Log in to GHCR for deploy
        run: |
          set -euo pipefail
          echo '${{ secrets.GHCR_TOKEN }}' | docker login ghcr.io -u '${{ github.repository_owner }}' --password-stdin

      - name: Deploy Docker stack
        shell: bash
        run: |
          set -euo pipefail
          set -a
          source ./envfile
          set +a
          docker --context remote stack deploy --compose-file apps/panel/docker-stack.yaml --with-registry-auth digital-signage
